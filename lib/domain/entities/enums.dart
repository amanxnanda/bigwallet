/// Type of message.
enum MessageType {
  error,
  success,
  information,
}

/// Yes or no choice
enum YesOrNo { yes, no }

/// From where the image should be picked
enum ImageOption {
  gallery,
  camera,
}